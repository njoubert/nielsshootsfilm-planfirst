# Environment Variables Template
# Copy this file to env and fill in your values
#
# Note: Both backend/ and frontend/ directories have symlinks pointing here:
#   backend/env  -> ../env
#   frontend/env -> ../env
# This ensures there is only one env file at the project root

# Directory paths (relative to project root)
DATA_DIR=./data
UPLOAD_DIR=./static/uploads

# Server configuration
PORT=6180

# Admin authentication
# For LOCAL DEVELOPMENT/TESTING:
#   - Set ADMIN_PASSWORD to a plain text password (e.g., "admin")
#   - It will be automatically hashed when the backend starts
#   - This makes local testing easier with a simple password
#
# For PRODUCTION:
#   - Remove or comment out ADMIN_PASSWORD
#   - Generate a hash: cd backend && go run cmd/hash-password/main.go YOUR_SECURE_PASSWORD
#   - Set ADMIN_PASSWORD_HASH to the generated hash
#   - Or set the hash in data/admin_config.json instead
#
# Priority: ADMIN_PASSWORD > ADMIN_PASSWORD_HASH > admin_config.json
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
ADMIN_PASSWORD_HASH=

# Frontend configuration (for development)
VITE_PUBLIC_API_URL=http://localhost:6180

# Development deployment
SITE_URL=nielsshootsfilm.com
HOST=localhost
ADMIN_SERVER_PORT=6180

# Deployment server configuration (for rsync deployment)
DEPLOY_USER=njoubert
DEPLOY_HOST=njoubert.com
DEPLOY_PATH=/var/www/nielsshootsfilm.com/

# Development mode
DEV_MODE=true

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=your-secret-key-here

# CORS settings (for development)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Image upload limits (in MB)
MAX_FILE_SIZE=100
MAX_BATCH_SIZE=5000

# Logging
LOG_LEVEL=info
LOG_FORMAT=json
